import Component from '@glimmer/component';

// This file intentionally left simple: the table rendering is handled by
// the HBS template in `ffg-skills-row.hbs` and row logic is implemented
// in `ffg-skill-row.js` (singular) so we don't duplicate row actions here.
export default class FfgSkillsRowComponent extends Component {}
